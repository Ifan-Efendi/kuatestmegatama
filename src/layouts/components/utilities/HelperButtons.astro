<div
  id="action-buttons"
  class="pointer-events-none fixed right-0 bottom-5 z-100 flex flex-col items-center text-center opacity-0 transition-all duration-300 [&.scroll-down]:pointer-events-auto [&.scroll-down]:right-5 [&.scroll-down]:opacity-100">
  <a
    href="https://wa.me/6289534457316"
    target="_blank"
    rel="noopener noreferrer"
    aria-label="WhatsApp"
    title="WhatsApp"
    class="inline-flex h-14 w-14 items-center justify-center rounded-2xl bg-[#25D366] shadow-lg transition-transform duration-200 hover:scale-105 focus:scale-105 sm:h-20 sm:w-20">
    <img
      src="/images/icons/whatsapp.svg"
      alt="WhatsApp"
      width="48"
      height="48"
      class="h-8 w-8 sm:h-12 sm:w-12" />
  </a>
</div>

<script>
  // Hide buttons in iframe
  function isInIframe() {
    try {
      return window.self !== window.top;
    } catch (e) {
      return true; // Access denied => definitely inside iframe
    }
  }

  if (!isInIframe()) {
    const btns = document.getElementById("action-buttons");
    if (btns) {
      btns.classList.remove("hidden"); // Show buttons only if not in iframe

      // Scroll-based show/hide
      window.addEventListener("scroll", () => {
        const currentScrollPos = window.scrollY;
        const windowHeight = window.innerHeight;
        const documentHeight = document.documentElement.scrollHeight;
        const distanceFromBottom =
          documentHeight - (currentScrollPos + windowHeight);

        if (currentScrollPos >= 500) {
          btns.classList.add("scroll-down");
        }

        if (currentScrollPos <= 500 || distanceFromBottom <= 100) {
          btns.classList.remove("scroll-down");
        }
      });

      // Add UTM ref tracking only when visible
      (() => {
        const LS_KEY = "checkout-ref";
        let ref = localStorage.getItem(LS_KEY);

        if (!ref) {
          ref = document.referrer || "direct";
          if (ref) localStorage.setItem(LS_KEY, ref);
        }

        if (!ref) return;

        btns.querySelectorAll("a").forEach((link) => {
          const separator = link.href.includes("?") ? "&" : "?";
          link.href += `${separator}utm_source=${encodeURIComponent(ref)}`;
        });
      })();
    }
  }
</script>
